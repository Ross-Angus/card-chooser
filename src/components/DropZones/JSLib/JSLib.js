import React from 'react';

// A drop zone to the left which represents JavaScript libraries
const JSLib = () => {
  return (
    <section aria-label="JavaScript library" className="dropzone jslib">
      <h2>JavaScript library</h2>
    </section>
  );
};

export default JSLib;
